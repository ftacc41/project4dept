mappings:
  # DAG run duration with dag_id as label
  - match: "airflow.dagrun.duration.success.*"
    name: "airflow_dagrun_duration_success_seconds"
    labels:
      dag_id: "$1"

  - match: "airflow.dagrun.duration.failed.*"
    name: "airflow_dagrun_duration_failed_seconds"
    labels:
      dag_id: "$1"

  - match: "airflow.dagrun.schedule_delay.*"
    name: "airflow_dagrun_schedule_delay_seconds"
    labels:
      dag_id: "$1"

  # Task operator successes/failures with operator as label
  - match: "airflow.operator_successes_*"
    name: "airflow_operator_successes_total"
    labels:
      operator: "$1"

  - match: "airflow.operator_failures_*"
    name: "airflow_operator_failures_total"
    labels:
      operator: "$1"

  # Pool metrics with pool name as label
  - match: "airflow.pool.open_slots.*"
    name: "airflow_pool_open_slots"
    labels:
      pool: "$1"

  - match: "airflow.pool.used_slots.*"
    name: "airflow_pool_used_slots"
    labels:
      pool: "$1"

  - match: "airflow.pool.starving_tasks.*"
    name: "airflow_pool_starving_tasks"
    labels:
      pool: "$1"

  # DAG processing times
  - match: "airflow.dag_processing.last_runtime.*"
    name: "airflow_dag_processing_last_runtime_seconds"
    labels:
      dag_file: "$1"

  # Catch-all: pass through remaining airflow metrics as-is
  - match: "airflow.*"
    name: "airflow_$1"
