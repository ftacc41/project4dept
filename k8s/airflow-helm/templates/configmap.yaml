apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "airflow-helm.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "airflow-helm.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: "{{ $value }}"
  {{- end }}